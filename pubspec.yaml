name: pdf_reader_manager
description: PDF Reader with C++ MuPDF backend
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  # permission_handler: ^11.0.1  # ŞİMDİLİK KALDIR
  file_picker: ^6.1.1    # ✅ EKLENDİ
  path_provider: ^2.1.1  # ✅ EKLENDİ

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
