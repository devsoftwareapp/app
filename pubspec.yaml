name: pdf_reader_manager
description: PDF Reader with Search
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.3.7
  pdfx: ^1.1.2  # YENİ EKLENDİ - advance_pdf_viewer yerine

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
